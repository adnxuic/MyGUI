{
  "name": "mygui-scanner-adapter",
  "version": "0.1.0",
  "description": "Dynamic Scanner Adapter support code for MyGUI: deterministic tool-name mapping, the fixed adapter host-code template, and scanner selection policy. Never contains scanner rules.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test \"tests/*.test.ts\"",
    "test:only": "node --test \"tests/*.test.ts\""
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
